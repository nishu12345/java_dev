class DataTypes{
	public static void main(String... s){
		boolean b = true;
		// boolean b1 = True; compile time error, symbol not found. As True not suitable for any datatype then compiler treat it as a variable.
		// boolean b2 = "True"; compile time error, incompatible types. String cannot be converted to boolean.
		// char ch = null; compile time error, incompatible types. <null> cannot be converted to char.
	}
}